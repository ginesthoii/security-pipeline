# security-pipeline
a reusable security pipeline you can drop into any GitHub repository. The workflow runs linters for Python and JavaScript, executes Bandit, Semgrep and Trivy, performs secret‑detection and fails the build when a high‑severity issue is found.
